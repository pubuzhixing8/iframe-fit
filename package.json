{
  "name": "iframe-fit",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.2.0",
  "scripts": {
    "build": "pnpm run clean && pnpm run build:parent && pnpm run build:child",
    "build:parent": "tsup packages/parent/index.ts --format esm --dts --outDir dist/parent",
    "build:child": "tsup packages/child/index.ts --format esm --dts --outDir dist/child",
    "clean": "rimraf dist",
    "lint": "pnpm run typecheck",
    "serve:examples": "pnpm run build && echo \"iframe-fit examples: http://localhost:8090/examples/basic/index.html\" && http-server . -p 8090 --cors -c-1 --silent",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "http-server": "^14.1.1",
    "rimraf": "^6.0.1",
    "tsup": "^8.3.5",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
