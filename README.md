# iframe-fit

A TypeScript-first reimplementation scaffold for the core `iframe-resizer` runtime.

## Scope

This first version focuses on the low-level packages only:

- `@iframe-fit/parent`
- `@iframe-fit/child`

The repository includes a preserved `legacy` source snapshot copied from `iframe-resizer`
to support gradual migration and module-by-module replacement.
